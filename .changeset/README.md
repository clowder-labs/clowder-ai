# Changesets

This project uses [changesets](https://github.com/changesets/changesets) for version management.

When making changes to published packages, run `pnpm changeset` to create a changeset describing your changes.
