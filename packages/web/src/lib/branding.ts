/**
 * Branding constants — neutral defaults for the open-source Core.
 * Edition builds override these at compile time via NEXT_PUBLIC_APP_NAME.
 */
export const APP_NAME = process.env.NEXT_PUBLIC_APP_NAME || 'Clowder AI';
